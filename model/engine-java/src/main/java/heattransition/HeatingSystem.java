package heattransition;

/** OL_HeatingSystem — order matches the AnyLogic enum / DB insert order (ordinal used for peer counts). */
public enum HeatingSystem {
    NATURAL_GAS_BOILER,
    NATURAL_GAS_BLOCK,
    HYBRID_HEAT_PUMP,
    ELECTRIC_HEAT_PUMP,
    DISTRICT_HEATING
}
