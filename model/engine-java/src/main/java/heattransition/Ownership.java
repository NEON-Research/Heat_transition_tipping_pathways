package heattransition;

/** OL_HouseholdOwnership (TOTAL is an output aggregate, not a stored value). */
public enum Ownership {
    PRIVATELY_OWNED,
    PRIVATELY_RENTED,
    SOCIAL_HOUSING,
    HOME_OWNER_ASSOCIATION
}
